<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.FastFlagCatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      mc:Ignorable="d"
      d:DesignHeight="1200" d:DesignWidth="800"
      Title="FastFlagCatalogPage"
      Scrollable="True"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.Menu_FastFlags_Catalog_Open_Title}" FontSize="20" FontWeight="Medium" />
        <TextBlock Margin="0,4,0,12" Text="{x:Static resources:Strings.Menu_FastFlags_Catalog_Open_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" Margin="0,0,8,0" PlaceholderText="{x:Static resources:Strings.Catalog_SearchHint}" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox Grid.Column="1" Width="180" Padding="10,5,10,5"
                      ItemsSource="{Binding CategoryLabels, Mode=OneTime}"
                      SelectedItem="{Binding SelectedCategoryLabel, Mode=TwoWay}" />
        </Grid>

        <WrapPanel Margin="0,10,0,0">
            <ui:Button Margin="0,0,6,6" Content="{x:Static resources:Strings.Catalog_Preset_Performance}" Command="{Binding PresetPerformanceCommand}" />
            <ui:Button Margin="0,0,6,6" Content="{x:Static resources:Strings.Catalog_Preset_LowLatency}" Command="{Binding PresetLowLatencyCommand}" />
            <ui:Button Margin="0,0,6,6" Content="{x:Static resources:Strings.Catalog_Preset_UIMinimal}" Command="{Binding PresetUIMinimalCommand}" />
            <ui:Button Margin="0,0,6,6" Icon="ArrowReset20" Content="{x:Static resources:Strings.Catalog_ResetAll}" Command="{Binding ResetAllCommand}" />
            <ui:Button Margin="0,0,6,6" Icon="DocumentArrowUp16" Content="{x:Static resources:Strings.Common_ImportFromFile}" Command="{Binding ImportCommand}" />
            <ui:Button Margin="0,0,6,6" Icon="DocumentArrowDown16" Content="{x:Static resources:Strings.Common_Export}" Command="{Binding ExportCommand}" />
        </WrapPanel>

        <TextBlock Margin="0,4,0,0" Text="{Binding StatusText, Mode=OneWay}" FontSize="12" Foreground="{DynamicResource SystemFillColorSuccessBrush}" />

        <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Items, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,8" Padding="12" CornerRadius="8" BorderThickness="1"
                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <Border Padding="6,1" CornerRadius="4" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                        <TextBlock FontSize="11" Text="{Binding CategoryLabel}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                    </Border>
                                    <TextBlock Margin="8,0,0,0" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" Text="{Binding FlagName}" />
                                    <Border Margin="8,0,0,0" Padding="6,1" CornerRadius="4" Background="{DynamicResource SystemFillColorSuccessBrush}" Visibility="{Binding ModifiedBadgeVisibility, Mode=OneWay}">
                                        <TextBlock FontSize="11" Text="{x:Static resources:Strings.Catalog_Modified}" Foreground="White" />
                                    </Border>
                                </StackPanel>

                                <ui:ToggleSwitch Grid.Column="1" IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
                            </Grid>

                            <TextBlock Grid.Row="1" Margin="0,6,0,0" FontSize="12" TextWrapping="Wrap" Text="{Binding Description}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />

                            <Grid Grid.Row="2" Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ui:TextBox Grid.Column="0" PlaceholderText="{x:Static resources:Strings.Common_Value}" Text="{Binding ValueText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEnabled, Mode=OneWay}" />

                                <ui:Button Grid.Column="1" Margin="6,0,0,0" Content="{x:Static resources:Strings.Common_OK}" Command="{Binding DataContext.ApplyValueCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" CommandParameter="{Binding}" IsEnabled="{Binding IsDirty, Mode=OneWay}" />

                                <ui:Button Grid.Column="2" Margin="6,0,0,0" Icon="ArrowReset20" Content="{x:Static resources:Strings.Catalog_ResetOne}" Command="{Binding DataContext.ResetFlagCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" CommandParameter="{Binding}" />
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Margin="0,8,0,0" Text="{x:Static resources:Strings.Catalog_NoResults}"
                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                   Visibility="{Binding NoResultsVisibility, Mode=OneWay}" />

        <TextBlock Margin="0,8,0,0" Text="{x:Static resources:Strings.Catalog_NetworkEmpty}" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
        <TextBlock Margin="0,8,0,0" Text="{x:Static resources:Strings.Catalog_PresetNote}" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
    </StackPanel>
</ui:UiPage>
