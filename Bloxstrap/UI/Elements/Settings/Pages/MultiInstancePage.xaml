<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.MultiInstancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="800"
      Title="MultiInstancePage"
      Scrollable="True"
      Loaded="Page_Loaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.MultiInstance_Title}" FontSize="20" FontWeight="Medium" />
        <TextBlock Margin="0,4,0,12" Text="{x:Static resources:Strings.MultiInstance_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <controls:OptionControl
            Header="{x:Static resources:Strings.MultiInstance_Enable_Title}"
            Description="{x:Static resources:Strings.MultiInstance_Enable_Description}">
            <ui:ToggleSwitch IsChecked="{Binding MultiInstanceLaunchingEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <ui:InfoBar Margin="0,8,0,0"
                    Title="{x:Static resources:Strings.Common_Note}"
                    Message="{x:Static resources:Strings.MultiInstance_Note}"
                    Severity="Informational"
                    IsOpen="True"
                    IsClosable="False" />

        <Grid Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" FontSize="20" FontWeight="Medium" VerticalAlignment="Center" Text="{x:Static resources:Strings.MultiInstance_RunningInstances}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Margin="0,0,6,0" Icon="ArrowSync16" Content="{x:Static resources:Strings.Common_Refresh}" Command="{Binding RefreshCommand}" />
                <ui:Button Appearance="Primary" Content="{Binding LaunchButtonText, Mode=OneWay}" Command="{Binding LaunchAnotherCommand}" IsEnabled="{Binding LaunchButtonsEnabled, Mode=OneWay}" />
            </StackPanel>
        </Grid>

        <TextBlock Margin="0,10,0,0" Text="{x:Static resources:Strings.MultiInstance_NoInstances}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Visibility="{Binding NoInstancesVisibility, Mode=OneWay}" />

        <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Instances, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,8" Padding="12" CornerRadius="8" BorderThickness="1"
                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Ellipse Grid.Column="0" Width="10" Height="10" Fill="{DynamicResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" Margin="10,0,8,0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontSize="14" FontWeight="Medium" Text="{Binding AccountLabel, Mode=OneWay}" />
                                    <Border Margin="8,0,0,0" Padding="6,1" CornerRadius="4" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                        <TextBlock FontSize="11" Text="{Binding StatusText, Mode=OneWay}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock FontSize="12" Text="{Binding StartedAtText, Mode=OneWay}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                            </StackPanel>

                            <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                                <Run Text="PID " /><Run Text="{Binding Pid, Mode=OneWay}" />
                            </TextBlock>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</ui:UiPage>
