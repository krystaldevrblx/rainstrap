<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.ClipsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="800"
      Title="ClipsPage"
      Scrollable="True"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.Clips_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <TextBlock Text="{x:Static resources:Strings.Clips_Status_Header}" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />

        <Border Padding="16" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
            <StackPanel>
                <TextBlock FontSize="14" FontWeight="Medium" Text="{Binding StatusText, Mode=OneWay}" />
                <TextBlock Margin="0,4,0,0" FontSize="12" Text="{Binding StatusDetail, Mode=OneWay}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <TextBlock Text="{x:Static resources:Strings.Common_Presets}" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />

        <controls:OptionControl
            Header="{x:Static resources:Strings.Clips_Enable_Title}"
            Description="{x:Static resources:Strings.Clips_Enable_Description}">
            <ui:ToggleSwitch IsChecked="{Binding ClipsEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Clips_Duration_Title}"
            Description="{x:Static resources:Strings.Clips_Duration_Description}">
            <ComboBox Width="200" Padding="10,5,10,5" ItemsSource="{Binding BufferDurations, Mode=OneTime}" Text="{Binding SelectedDuration, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Clips_Fps_Title}"
            Description="{x:Static resources:Strings.Clips_Fps_Description}">
            <ComboBox Width="200" Padding="10,5,10,5" ItemsSource="{Binding CaptureFpsOptions, Mode=OneTime}" Text="{Binding SelectedFps, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Clips_Hotkey_Title}"
            Description="{x:Static resources:Strings.Clips_Hotkey_Description}">
            <ui:TextBox Width="200" Text="{Binding HotkeyText, Mode=TwoWay}" IsReadOnly="True" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Clips_OutputFolder_Title}"
            Description="{x:Static resources:Strings.Clips_OutputFolder_Description}">
            <Grid Width="300">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBox Grid.Column="0" Text="{Binding OutputFolder, Mode=TwoWay}" IsReadOnly="True" />
                <ui:Button Grid.Column="1" Margin="8,0,0,0" Width="34" Height="30" Icon="Folder16" Command="{Binding BrowseOutputFolderCommand}" />
            </Grid>
        </controls:OptionControl>

        <Grid Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="0" Appearance="Primary" Icon="Save24" Content="{x:Static resources:Strings.Clips_SaveClip}"
                       Command="{Binding SaveClipCommand}" IsEnabled="{Binding CanSaveClip, Mode=OneWay}"
                       VerticalAlignment="Center" />

            <ui:Button Grid.Column="1" Icon="Folder24" Content="{x:Static resources:Strings.Clips_OpenFolder}"
                       Command="{Binding OpenClipsFolderCommand}" VerticalAlignment="Center" Margin="12,0,0,0" />
        </Grid>

        <TextBlock Text="{x:Static resources:Strings.Clips_Limitations_Header}" FontSize="20" FontWeight="Medium" Margin="0,24,0,0" />
        <TextBlock Text="{x:Static resources:Strings.Clips_Limitations_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
    </StackPanel>
</ui:UiPage>
